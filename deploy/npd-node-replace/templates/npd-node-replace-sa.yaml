apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "npd-node-replace.fullname" . }}-sa
  namespace: {{ .Release.Namespace }}
  labels:
  {{- include "npd-node-replace.labels" . | nindent 4 }}
  annotations:
    {{- toYaml .Values.sa.serviceAccount.annotations | nindent 4 }}
